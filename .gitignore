Downloads/
node_modules
tests
Agent.spec.tests
// tests/agent.spec.ts
import { test, expect } from '@playwright/test';

test('Agent endpoint should respond with 200', async ({ request }) => {
  const response = await request.get(process.env.AGENT_BASE_URL + '/api/agent');
  expect(response.status()).toBe(200);
});

.vercel
synsuretech-demo-backup.zip
synsuretech-demo-backup.zip
